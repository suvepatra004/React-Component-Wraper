import { describe, it, expect } from "vitest";

describe("math", () => {
    it("adds correctly", () => {
        expect(2 + 3).toBe(5);
    });

    it("multiplies correctly", () => {
        expect(4 * 5).toBe(20);
    });
});
